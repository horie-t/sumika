# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

`sumika` is a household-budget / personal-finance management SaaS (家計簿管理のSaaSサービス).

License: BSD 3-Clause.

## Current state

The repository is in its initial state — only `README.md` and `LICENSE` exist. There is no
application code, build tooling, dependency manifest, test suite, or CI configuration yet.

Once the stack is chosen and scaffolded, update this file with:
- Build / run / lint / test commands (including how to run a single test).
- High-level architecture that spans multiple files (data model, service boundaries,
  auth, and how the SaaS tenancy is structured).
